%nav.white{:role => "navigation"}
  .nav-wrapper.container
    %a#logo-container.brand-logo{:href => "#"} BlogApp
    %ul.right.hide-on-med-and-down
      %li
        - if user_signed_in?
          .user_nav.grid-6
            = link_to "Logout", destroy_user_session_path, method: :delete
        - else
          .grid-6
            = link_to "Signup", new_user_registration_path, class: 'Singup'
